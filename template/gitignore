# Dependencies
node_modules/

# Superpowers brainstorming
.superpowers/
.pnp
.pnp.js

# Testing
coverage/
*.lcov
.nyc_output

# Production builds
dist/
build/
out/
.next/
# The e2e stack's build output (next.config.js sets distDir when E2E_BUILD=true,
# so an e2e run never clobbers the dev server's .next).
.next-e2e/
*.tsbuildinfo
next-env.d.ts

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# LLM call dumps (dev observability — enabled via ASSISTANT_DUMP_LLM_CALLS=1)
apps/api/.llm-dumps/

# Environment variables
.env
# Local overrides for the e2e stack (copy of env.e2e.example). Carries database
# credentials and the seeded administrator's password — never commit it.
.env.e2e
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.DS_Store

# Turborepo
.turbo/

# Vercel
.vercel

# Playwright
playwright-report/
playwright/.cache/
# storageState files: these hold LIVE session tokens for the seeded personas.
# Never commit them, never publish them, never attach one to a bug report.
playwright/.auth/
apps/web/playwright/.auth/
test-results/

# Debug
debug/
.vscode-test

# Misc
*.pem
.cache
.parcel-cache
.nuxt
.cache/
.vuepress/dist
.temp
.docusaurus

# Lock files (keep only pnpm-lock.yaml)
package-lock.json
yarn.lock

# Backup files
api-git-backup*.tar.gz
.env-backup/

# OS files
Thumbs.db
.DS_Store

# Temporary files
tmp/
temp/

# REST test files
REST.http

# Git worktrees
.worktrees/
.claude/worktrees/

# Core update workflow
.core-update-manifest.json

.playwright-cli/